apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-fraud-v1
  namespace: monkeyswork-ai
  labels:
    app: ai-fraud-v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ai-fraud-v1
  template:
    metadata:
      labels:
        app: ai-fraud-v1
    spec:
      serviceAccountName: ai-fraud-v1
      tolerations:
        - key: workload-type
          value: ai
          effect: NoSchedule
      nodeSelector:
        workload-type: ai
      containers:
        - name: ai-fraud-v1
          image: IMAGE_PLACEHOLDER
          ports:
            - containerPort: 8080
          env:
            - name: ENVIRONMENT
              value: "prod"
            - name: GCP_PROJECT_ID
              value: "monkeyswork-prod"
            - name: REGION
              value: "us-central1"
            - name: VERTEX_MODEL
              value: "gemini-3-flash-preview"
            - name: INTERNAL_API_URL
              value: "http://monkeyswork-api.monkeyswork.svc.cluster.local/api/v1/internal"
            - name: INTERNAL_API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: internal-api-token
                  key: token
            - name: FEATURE_FLAG_FRAUD_SCORING
              value: "true"
            - name: FRAUD_ENFORCEMENT_MODE
              value: "shadow"
            - name: LOG_LEVEL
              value: "info"
          resources:
            requests:
              cpu: 500m
              memory: 1Gi
            limits:
              cpu: "2"
              memory: 4Gi
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 15
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: ai-fraud-v1
  namespace: monkeyswork-ai
spec:
  selector:
    app: ai-fraud-v1
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP
