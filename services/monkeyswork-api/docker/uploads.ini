upload_max_filesize=25M
post_max_size=30M
max_file_uploads=20

; Prevent premature stdout output from corrupting HTTP responses
; The ErrorHandler and loggers can echo/print before SapiEmitter sends headers;
; output_buffering captures this output so SapiEmitter can clean it first.
output_buffering=4096
display_errors=Off
