apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: monkeyswork-ingress
  namespace: monkeyswork
  annotations:
    kubernetes.io/ingress.class: "gce"
    kubernetes.io/ingress.global-static-ip-name: "mw-prod-ingress-ip"
    networking.gke.io/managed-certificates: "mw-certs"
    kubernetes.io/ingress.allow-http: "false"
spec:
  rules:
    # Frontend — monkeyswork.com
    - host: monkeyswork.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 80
    # Frontend — www.monkeyswork.com
    - host: www.monkeyswork.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 80
    # API — api.monkeyswork.com
    - host: api.monkeyswork.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: monkeyswork-api
                port:
                  number: 80
    # WebSocket — ws.monkeyswork.com
    - host: ws.monkeyswork.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: socket-server
                port:
                  number: 80
---
apiVersion: networking.gke.io/v1
kind: ManagedCertificate
metadata:
  name: mw-certs
  namespace: monkeyswork
spec:
  domains:
    - monkeyswork.com
    - www.monkeyswork.com
    - api.monkeyswork.com
    - ws.monkeyswork.com
