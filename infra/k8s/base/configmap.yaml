apiVersion: v1
kind: ConfigMap
metadata:
  name: feature-flags
  namespace: monkeyswork
data:
  AI_SCOPE_ENABLED: "true"
  AI_MATCH_ENABLED: "true"
  FRAUD_SCORING_ENABLED: "true"
  AUTO_VERIFICATION_ENABLED: "true"
  AB_TEST_MATCH_PERCENT: "0"
  FRAUD_ENFORCEMENT_MODE: "shadow"
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: feature-flags
  namespace: monkeyswork-ai
data:
  AI_SCOPE_ENABLED: "true"
  AI_MATCH_ENABLED: "true"
  FRAUD_SCORING_ENABLED: "true"
  FRAUD_ENFORCEMENT_MODE: "shadow"
  AB_TEST_MATCH_PERCENT: "0"
---
# Shared production config for all AI Python services
apiVersion: v1
kind: ConfigMap
metadata:
  name: ai-service-config
  namespace: monkeyswork-ai
data:
  ENVIRONMENT: "prod"
  GCP_PROJECT_ID: "monkeyswork-prod"
  REGION: "us-central1"
  VERTEX_MODEL: "gemini-3-flash-preview"
  INTERNAL_API_URL: "http://monkeyswork-api.monkeyswork.svc.cluster.local/api/v1/internal"
  LOG_LEVEL: "info"
---
# Same config for verification-automation (lives in monkeyswork namespace)
apiVersion: v1
kind: ConfigMap
metadata:
  name: ai-service-config
  namespace: monkeyswork
data:
  ENVIRONMENT: "prod"
  GCP_PROJECT_ID: "monkeyswork-prod"
  REGION: "us-central1"
  VERTEX_MODEL: "gemini-3-flash-preview"
  INTERNAL_API_URL: "http://monkeyswork-api.monkeyswork.svc.cluster.local/api/v1/internal"
  LOG_LEVEL: "info"
